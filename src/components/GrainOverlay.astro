<div class="grain"></div>

<style>
    .grain {
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        width: 100%;
        height: 100%;
        background-image: url("images/grain_256.png");
        background-repeat: repeat;
        animation: grain-animation 0.3s steps(10) infinite;
        mix-blend-mode: hard-light;
        opacity: 0.2;
        z-index: -1;
        pointer-events: none;
    }

    @keyframes grain-animation {
        0% { background-position: 0% 0%; }
        100% { background-position: 100% 100%; }
    }
</style>