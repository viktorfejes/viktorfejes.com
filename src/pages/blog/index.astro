---
import Layout from "../../layouts/Global.astro";
import Badge from "../../components/Badge.astro";

import { getCollection } from 'astro:content';
import FormattedDate from '../../components/FormattedDate.astro';
import { Image } from 'astro:assets';

const posts = (await getCollection('blog')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
);
---

<Layout title="Articles - Viktor Fejes">
    <div class="w-full max-w-xl">
        <ul class="flex flex-col gap-4">
            <li>
                <a href="/blog/vector-operations" class="sm:flex justify-between items-center gap-4">
                    <h4 class="text-lg font-medium dark:text-neutral-300">Vector Operations: A Visual Guide</h4>
                    <div class="bg-neutral-200 h-px flex-grow hidden sm:block"></div>
                    <p class="">
                        Jul 31, 2025
                    </p>
                </a>
            </li>
            <li>
                <a href="#" class="sm:flex justify-between items-center gap-4">
                    <h4 class="text-lg font-medium dark:text-neutral-300">Compute Shaders <Badge class="bg-neutral-200 dark:bg-neutral-800">Coming soon</Badge></h4>
                    <div class="bg-neutral-200 h-px flex-grow hidden sm:block"></div>
                    <p class="">
                        Aug 2025
                    </p>
                </a>
            </li>
            <li>
                <a href="#" class="sm:flex justify-between items-center gap-4">
                    <h4 class="text-lg font-medium dark:text-neutral-300">Understanding RGB Colors <Badge class="bg-neutral-200 dark:bg-neutral-800">Coming soon</Badge></h4>
                    <div class="bg-neutral-200 h-px flex-grow hidden sm:block"></div>
                    <p class="">
                        Sep 2025
                    </p>
                </a>
            </li>
            <!-- { -->
            <!--     posts.map((post) => ( -->
            <!--         <li class="mb-2"> -->
            <!--             <a href={`/blog/${post.id}/`} class="flex justify-between items-center gap-4"> -->
            <!--                 <h4 class="text-lg font-medium dark:text-neutral-300">{post.data.title}</h4> -->
            <!--                 <div class="bg-neutral-200 h-px flex-grow"></div> -->
            <!--                 <p class=""> -->
            <!--                     <FormattedDate date={post.data.pubDate} /> -->
            <!--                 </p> -->
            <!--             </a> -->
            <!--         </li> -->
            <!--     )) -->
            <!-- } -->
        </ul>
    </div>
</Layout>
